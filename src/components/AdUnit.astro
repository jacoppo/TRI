---
interface Props {
  slot?: 'leaderboard' | 'rectangle' | 'skyscraper';
  slotId: string;
  label?: string;
  class?: string;
}

const {
  slot: adSlot = 'rectangle',
  slotId,
  label = 'Pubblicità',
  class: className = '',
} = Astro.props;

const isDev = import.meta.env.DEV;

const adClass = {
  leaderboard: 'ad-leaderboard',
  rectangle: 'ad-rectangle',
  skyscraper: 'ad-skyscraper',
}[adSlot];
---

<div class={`ad-unit ${adClass} ${className}`}>
  {isDev ? (
    <div class="ad-unit-inner">{label}</div>
  ) : (
    <ins
      class="adsbygoogle"
      style="display:block"
      data-ad-client="ca-pub-9090975380946777"
      data-ad-slot={slotId}
      data-ad-format="auto"
      data-full-width-responsive="true"
    ></ins>
    <script type="text/partytown">
      (adsbygoogle = window.adsbygoogle || []).push({});
    </script>
  )}
</div>
